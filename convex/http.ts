import { httpRouter } from "convex/server";

const http = httpRouter();

// Auth routes are now handled by Better Auth via Next.js API routes
// This file is kept for any future Convex HTTP endpoints

export default http;
